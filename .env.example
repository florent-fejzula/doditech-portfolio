# Firebase — only needed if you want contact-form messages written to
# Firestore. Leave unset and the form falls back to the visitor's mail
# client, which works fine on day one.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
